# PUBLIC, disposable local credentials ONLY. Never reuse outside this stack.
POSTGRES_PASSWORD=musemint_local_only
# Reserved for the future backend (not consumed by the current tooling).
DATABASE_URL=postgresql://musemint:musemint_local_only@127.0.0.1:5432/musemint
RPC_URL=http://127.0.0.1:8545
CHAIN_ID=31337
# No provider tokens, signing keys, or JWT secrets needed for current checks.
